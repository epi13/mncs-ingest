# mncs-ingest
Machine-native data ingestion and semantic normalization for MNCS, converting external signals into canonical typed structures, relations, and graph-ready representations for memory, models, agents, and runtime systems.
